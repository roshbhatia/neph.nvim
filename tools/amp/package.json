{
  "name": "neph-amp-bridge",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/amp.js",
  "scripts": {
    "build": "esbuild neph-plugin.ts --bundle --platform=node --format=esm --outfile=dist/amp.js --external:@ampcode/plugin"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.19.0",
    "vitest": "^1.0.0"
  }
}
