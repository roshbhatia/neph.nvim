{
  "name": "neph-opencode-bridge",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/opencode.js",
  "scripts": {
    "build": "esbuild opencode.ts --bundle --platform=node --format=esm --outfile=dist/opencode.js --external:@opencode-ai/plugin --external:neovim"
  },
  "dependencies": {
    "neovim": "^5.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "vitest": "^1.0.0"
  }
}
