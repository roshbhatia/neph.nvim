{
  "name": "pi-nvim-extension",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/pi.js",
  "exports": {
    ".": "./dist/pi.js"
  },
  "scripts": {
    "build": "esbuild pi.ts --bundle --platform=node --format=esm --outfile=dist/pi.js --external:@mariozechner/pi-coding-agent",
    "test": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "vitest": "^1.0.0"
  }
}
