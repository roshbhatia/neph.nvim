{
  "name": "gemini-companion",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/companion.js",
  "scripts": {
    "build": "esbuild src/companion.ts --bundle --platform=node --format=esm --outfile=dist/companion.js --external:neovim",
    "test": "vitest"
  },
  "dependencies": {
    "neovim": "^5.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "vitest": "^1.0.0"
  }
}
