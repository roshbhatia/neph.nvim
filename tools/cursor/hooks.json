{
  "hooks": {
    "afterFileEdit": [
      {
        "command": "PATH=$HOME/.local/bin:$PATH neph integration hook cursor"
      }
    ],
    "beforeShellExecution": [
      {
        "command": "PATH=$HOME/.local/bin:$PATH neph integration hook cursor"
      }
    ],
    "beforeMCPExecution": [
      {
        "command": "PATH=$HOME/.local/bin:$PATH neph integration hook cursor"
      }
    ]
  }
}
