[project]
name = "neph-shim"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["msgpack>=1.0", "click>=8.0"]

[dependency-groups]
dev = [
  "pytest>=8.0",
  "msgpack>=1.0",
  "click>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
